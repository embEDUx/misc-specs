run mmcargs
setenv bootargs ${bootargs} net.ifnames=1

setenv bootfile zImage
setenv kernel_addr_r 0x81000000
run findfdt

fatload mmc ${mmcdev} ${kernel_addr_r} zImage
fatload mmc ${mmcdev} ${fdtaddr} ${fdtfile}
bootz ${kernel_addr_r} - ${fdtaddr}
